name: ubuntu.local
os: linux
arch: amd64
host: ubuntu.local
user: jkh
driver: nixpkgs
nix_via_docker: true
python: /usr/bin/python3
# Linux build worker (Ubuntu 24.04 LTS).
# Runs nixpkgs driver builds via Docker (nixos/nix container).
# Docker available; jkh in docker group.
