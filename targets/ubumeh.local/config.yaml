name: ubumeh.local
os: linux
arch: amd64
host: ubumeh.local
user: jkh
# S3/MinIO artifact storage host — not a build worker.
# MinIO running in Docker:
#   container: minio
#   API:     http://ubumeh.local:9000
#   Console: http://ubumeh.local:9001
#   Bucket:  theseus-artifacts
# Credentials in config.yaml (gitignored) under artifact_store.
