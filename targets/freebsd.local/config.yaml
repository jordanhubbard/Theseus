name: freebsd.local
os: freebsd
arch: amd64
host: freebsd.local
user: jkh
ports_root: /usr/ports
python: /usr/local/bin/python3
# OS/arch-specific build behavior — to be evolved as testing proceeds.
# Known: ports tree at /usr/ports, Python 3.11 at /usr/local/bin/python3.
# Nix installed in single-user mode at /nix.
