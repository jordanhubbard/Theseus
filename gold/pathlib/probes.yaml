family: pathlib
backend: python_module
module: pathlib
probes:
- id: purepath_name
  function: PurePosixPath
  args:
  - /etc/passwd
  method: name
  expect:
    eq: passwd
- id: purepath_stem
  function: PurePosixPath
  args:
  - archive.tar.gz
  method: stem
  expect:
    eq: archive.tar
