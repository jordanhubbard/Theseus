family: pathspec
reviewed: true
reviewed_at: "2026-09-20"
reviewer: theseus-goal-pass
status: accepted
items:
  - id: pathspec.match_file
    claim: "from_lines('gitwildmatch', ['*.py']).match_file('hello.py') is True; hello.txt is False."
    status: resolved
    evidence: "pathspec docs; gitignore spec; live probe"
  - id: pathspec.regex_pattern
    claim: "Exact GitWildMatchPattern.regex.pattern string is an implementation artifact kept only as a Layer 2 pin, not a public-doc claim."
    status: deferred
    evidence: "pathspec docs do not publish the compiled regex"
