family: tomllib
backend: python_module
module: tomllib
probes:
- id: int
  function: loads
  args:
  - x = 42
  expect:
    eq:
      x: 42
- id: bad
  function: loads
  args:
  - '='
  expect:
    raises: TOMLDecodeError
