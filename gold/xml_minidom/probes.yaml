family: xml_minidom
backend: python_module
module: xml.dom.minidom
probes:
- id: xml_minidom_childnodes_class
  function: parseString
  args:
  - <a/>
  method: childNodes.__class__.__name__
  expect:
    eq: NodeList
- id: xml_minidom_childnodes_len
  function: parseString
  args:
  - <a/>
  method: childNodes.length
  expect:
    eq: 1
