family: datetime_timedelta
reviewed: true
reviewed_at: '2026-09-21'
reviewer: theseus-medium-pass
status: accepted
items:
- id: datetime_timedelta.datetime_timedelta_weeks_to_days
  claim: timedelta(weeks=1).days == 7 — weeks kwarg is normalised to days
  status: resolved
  evidence: public Layer 2 oracle; live probe
- id: datetime_timedelta.datetime_timedelta_hours_to_seconds
  claim: timedelta(hours=1).seconds == 3600 — hours kwarg stored as sub-day seconds
  status: resolved
  evidence: public Layer 2 oracle; live probe
- id: datetime_timedelta.remainder
  claim: I/O, process-global state, and error paths outside the probed calls are out
    of scope.
  status: deferred
  evidence: ADR 0003 characterization scope for this pass
