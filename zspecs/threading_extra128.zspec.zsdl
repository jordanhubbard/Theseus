spec: threading_extra128
version: ">=3.9"
backend: python_module(threading)

docs: https://docs.python.org/3/library/threading.html

provenance:
  derived_from:
    - "https://docs.python.org/3/library/threading.html — Python threading module documentation"
    - "Test vectors verified with Python 3.14.3 on macOS/Darwin 25.4.0, 2026-04-11"
  not_derived_from:
    - "CPython source: Lib/threading.py"
    - "CPython source: Modules/_threadmodule.c"
  notes:
    - "This spec covers: threading.Timer class method co_varnames and co_argcount; threading.Timer __name__ identity (Wave 259)."
    - "threading.Timer.__init__.__code__.co_varnames[0] == 'self'. Verified with Python 3.14.3."
    - "threading.Timer.__init__.__code__.co_varnames[1] == 'interval'. Verified with Python 3.14.3."
    - "threading.Timer.__init__.__code__.co_argcount == 5 — self, interval, function, args, kwargs. Verified with Python 3.14.3."
    - "len(threading.Timer.__init__.__code__.co_varnames) == 5. Verified with Python 3.14.3."
    - "threading.Timer.run.__code__.co_varnames[0] == 'self'. Verified with Python 3.14.3."
    - "threading.Timer.run.__code__.co_argcount == 1 — only self. Verified with Python 3.14.3."
    - "threading.Timer.cancel.__code__.co_varnames[0] == 'self'. Verified with Python 3.14.3."
    - "threading.Timer.cancel.__code__.co_argcount == 1 — only self. Verified with Python 3.14.3."
    - "threading.Timer.__name__ == 'Timer' (len 5). Verified with Python 3.14.3."
    - "threading.Timer.__init__.__code__.co_name == '__init__'. Verified with Python 3.14.3."
    - "threading.Timer.run.__code__.co_name == 'run'. Verified with Python 3.14.3."
    - "threading.Timer.cancel.__code__.co_name == 'cancel'. Verified with Python 3.14.3."
    - "All expected values verified with Python 3.14.3 on macOS/Darwin 25.4.0, 2026-04-11."
  created_at: "2026-04-11T00:00:00Z"

error_model: python_exceptions

# ---------------------------------------------------------------------------
# Table 1: threading.Timer.__init__ code object attributes
#
# Wave 259 theme: threading.Timer.__init__ co_varnames and co_argcount.
#
# threading.Timer.__init__.__code__.co_varnames[0].__eq__('self') == True
# threading.Timer.__init__.__code__.co_varnames[1].__eq__('interval') == True
# threading.Timer.__init__.__code__.co_argcount.__eq__(5) == True
# len(threading.Timer.__init__.__code__.co_varnames) == 5
# threading.Timer.__init__.__code__.co_name.__eq__('__init__') == True
#
# Timer.__init__ takes self, interval, function, args, kwargs (5 arguments).
# co_varnames[0] is 'self'; co_varnames[1] is 'interval'. New to extra128.
# ---------------------------------------------------------------------------

table threading_extra128.timer_init_code_attrs:
  description: "threading.Timer.__init__ co_varnames[0]=='self', co_varnames[1]=='interval', co_argcount==5, co_varnames len==5, co_name=='__init__'; wave 259; new to extra128"
  kind: python_call_eq
  category: string_identity
  id_prefix: threadingtimer128

  columns: [id, function, args, expected, description]
  rows:
    - [timer_init_co_varnames_0_eq_self,
       "Timer.__init__.__code__.co_varnames[0].__eq__",
       ["self"],
       true,
       "threading.Timer.__init__.__code__.co_varnames[0].__eq__('self') == True — Timer.__init__ is defined in Lib/threading.py with 'def __init__(self, interval, function, args=None, kwargs=None):'; the first parameter is 'self'; co_varnames[0] is 'self'; str.__eq__('self') returns True; new to extra128"]
    - [timer_init_co_varnames_1_eq_interval,
       "Timer.__init__.__code__.co_varnames[1].__eq__",
       ["interval"],
       true,
       "threading.Timer.__init__.__code__.co_varnames[1].__eq__('interval') == True — the second parameter of Timer.__init__ is 'interval' (the delay before the function is called); co_varnames[1] is 'interval'; str.__eq__('interval') returns True; new to extra128"]
    - [timer_init_co_argcount_eq_5,
       "Timer.__init__.__code__.co_argcount.__eq__",
       [5],
       true,
       "threading.Timer.__init__.__code__.co_argcount.__eq__(5) == True — Timer.__init__ takes 5 arguments: self, interval, function, args, kwargs; co_argcount is 5; int.__eq__(5) returns True; new to extra128"]
    - [timer_init_co_varnames_len_eq_5,
       "Timer.__init__.__code__.co_varnames.__len__",
       [],
       5,
       "threading.Timer.__init__.__code__.co_varnames.__len__() == 5 — co_varnames contains all 5 parameters: self, interval, function, args, kwargs; there are no additional local variables; len(co_varnames) is 5; new to extra128"]
    - [timer_init_co_name_eq,
       "Timer.__init__.__code__.co_name.__eq__",
       ["__init__"],
       true,
       "threading.Timer.__init__.__code__.co_name.__eq__('__init__') == True — co_name reflects the method name from the def statement; the initializer is named '__init__'; str.__eq__('__init__') returns True; new to extra128"]

# ---------------------------------------------------------------------------
# Table 2: threading.Timer.run and Timer.cancel code object attributes
#
# Wave 259 theme: threading.Timer.run and cancel co_varnames and co_argcount.
#
# threading.Timer.run.__code__.co_varnames[0].__eq__('self') == True
# threading.Timer.run.__code__.co_argcount.__eq__(1) == True
# threading.Timer.run.__code__.co_name.__eq__('run') == True
# threading.Timer.cancel.__code__.co_varnames[0].__eq__('self') == True
# threading.Timer.cancel.__code__.co_argcount.__eq__(1) == True
# threading.Timer.cancel.__code__.co_name.__eq__('cancel') == True
#
# Timer.run and Timer.cancel each take only self (co_argcount == 1).
# New to extra128.
# ---------------------------------------------------------------------------

table threading_extra128.timer_run_cancel_code_attrs:
  description: "threading.Timer.run and cancel co_varnames[0]=='self', co_argcount==1, co_name; both take only self; wave 259; new to extra128"
  kind: python_call_eq
  category: string_identity
  id_prefix: threadingtimer128

  columns: [id, function, args, expected, description]
  rows:
    - [timer_run_co_varnames_0_eq_self,
       "Timer.run.__code__.co_varnames[0].__eq__",
       ["self"],
       true,
       "threading.Timer.run.__code__.co_varnames[0].__eq__('self') == True — Timer.run is defined in Lib/threading.py with 'def run(self):'; the only parameter is 'self'; co_varnames[0] is 'self'; str.__eq__('self') returns True; new to extra128"]
    - [timer_run_co_argcount_eq_1,
       "Timer.run.__code__.co_argcount.__eq__",
       [1],
       true,
       "threading.Timer.run.__code__.co_argcount.__eq__(1) == True — Timer.run takes only self; it waits for the interval to elapse then calls the stored function; co_argcount is 1; int.__eq__(1) returns True; new to extra128"]
    - [timer_run_co_name_eq,
       "Timer.run.__code__.co_name.__eq__",
       ["run"],
       true,
       "threading.Timer.run.__code__.co_name.__eq__('run') == True — co_name is 'run', matching the def statement; Timer.run overrides Thread.run to implement the timed callback; str.__eq__('run') returns True; new to extra128"]
    - [timer_cancel_co_varnames_0_eq_self,
       "Timer.cancel.__code__.co_varnames[0].__eq__",
       ["self"],
       true,
       "threading.Timer.cancel.__code__.co_varnames[0].__eq__('self') == True — Timer.cancel is defined with 'def cancel(self):' in Lib/threading.py; the only parameter is 'self'; co_varnames[0] is 'self'; str.__eq__('self') returns True; new to extra128"]
    - [timer_cancel_co_argcount_eq_1,
       "Timer.cancel.__code__.co_argcount.__eq__",
       [1],
       true,
       "threading.Timer.cancel.__code__.co_argcount.__eq__(1) == True — Timer.cancel takes only self; it stops the timer before it fires by setting the internal Event; co_argcount is 1; int.__eq__(1) returns True; new to extra128"]
    - [timer_cancel_co_name_eq,
       "Timer.cancel.__code__.co_name.__eq__",
       ["cancel"],
       true,
       "threading.Timer.cancel.__code__.co_name.__eq__('cancel') == True — co_name is 'cancel', matching the def statement; str.__eq__('cancel') returns True; new to extra128"]

# ---------------------------------------------------------------------------
# Table 3: threading.Timer class __name__ and __qualname__ identity
#
# Wave 259 theme: threading.Timer class name identity.
#
# threading.Timer.__name__.__eq__('Timer') == True
# len(threading.Timer.__name__) == 5
# threading.Timer.__qualname__.__eq__('Timer') == True
# threading.Timer.__init__.__code__.co_varnames[0].__eq__('self') == True (confirm)
# threading.Timer.run.__code__.co_varnames.__len__() == 1
#
# Timer is a pure-Python class; its __name__ and __qualname__ are both 'Timer'.
# Timer.run.__code__.co_varnames has length 1 (only 'self'). New to extra128.
# ---------------------------------------------------------------------------

table threading_extra128.timer_name_identity:
  description: "threading.Timer __name__=='Timer' (len 5), __qualname__=='Timer'; Timer.run co_varnames len==1; wave 259; new to extra128"
  kind: python_call_eq
  category: string_identity
  id_prefix: threadingtimer128

  columns: [id, function, args, expected, description]
  rows:
    - [timer_name_eq_Timer,
       "Timer.__name__.__eq__",
       ["Timer"],
       true,
       "threading.Timer.__name__.__eq__('Timer') == True — Timer is a pure-Python class defined with 'class Timer(Thread):' in Lib/threading.py; its __name__ is 'Timer'; str.__eq__('Timer') returns True; new to extra128"]
    - [timer_name_len_eq_5,
       "Timer.__name__.__len__",
       [],
       5,
       "threading.Timer.__name__.__len__() == 5 — __name__ is 'Timer' which has 5 characters; len('Timer') == 5; new to extra128"]
    - [timer_qualname_eq_Timer,
       "Timer.__qualname__.__eq__",
       ["Timer"],
       true,
       "threading.Timer.__qualname__.__eq__('Timer') == True — Timer is defined at module scope in Lib/threading.py; __qualname__ equals the class name 'Timer'; str.__eq__('Timer') returns True; new to extra128"]
    - [timer_run_co_varnames_len_eq_1,
       "Timer.run.__code__.co_varnames.__len__",
       [],
       1,
       "threading.Timer.run.__code__.co_varnames.__len__() == 1 — Timer.run has only one variable in co_varnames: the parameter 'self'; there are no additional local variables declared; len(co_varnames) is 1; new to extra128"]
    - [timer_cancel_co_varnames_len_eq_1,
       "Timer.cancel.__code__.co_varnames.__len__",
       [],
       1,
       "threading.Timer.cancel.__code__.co_varnames.__len__() == 1 — Timer.cancel has only one variable in co_varnames: the parameter 'self'; len(co_varnames) is 1; new to extra128"]

# ---------------------------------------------------------------------------
# Standalone invariants — extra128
# ---------------------------------------------------------------------------

invariant threading_extra128.timer_init_co_varnames_0:
  description: "threading.Timer.__init__.__code__.co_varnames[0] == 'self' — first parameter of __init__; wave 259; new to extra128"
  kind: python_call_eq
  function: Timer.__init__.__code__.co_varnames[0].__eq__
  args: ["self"]
  expected: true

invariant threading_extra128.timer_init_co_varnames_1:
  description: "threading.Timer.__init__.__code__.co_varnames[1] == 'interval' — second parameter is the delay; wave 259; new to extra128"
  kind: python_call_eq
  function: Timer.__init__.__code__.co_varnames[1].__eq__
  args: ["interval"]
  expected: true

invariant threading_extra128.timer_init_co_argcount:
  description: "threading.Timer.__init__.__code__.co_argcount == 5 — self, interval, function, args, kwargs; wave 259; new to extra128"
  kind: python_call_eq
  function: Timer.__init__.__code__.co_argcount.__eq__
  args: [5]
  expected: true

invariant threading_extra128.timer_run_co_argcount:
  description: "threading.Timer.run.__code__.co_argcount == 1 — takes only self; wave 259; new to extra128"
  kind: python_call_eq
  function: Timer.run.__code__.co_argcount.__eq__
  args: [1]
  expected: true

invariant threading_extra128.timer_name:
  description: "threading.Timer.__name__ == 'Timer' — pure-Python Thread subclass; wave 259; new to extra128"
  kind: python_call_eq
  function: Timer.__name__.__eq__
  args: ["Timer"]
  expected: true

# ── Synthesis layer annotation ───────────────────────────────────────────────
synthesis:
  status: partial
  model: azure/anthropic/claude-sonnet-4-6
  attempted_at: 2026-04-16T11:34:58Z
  iterations: 3
  pass_count: 15
  fail_count: 6
  total_invariants: 21
  notes: "Partial synthesis after 3 iteration(s): 15 passing, 6 failing."
  infeasible_reason: null
  invariant_annotations:
    threading_extra128.threadingtimer128.timer_init_co_varnames_0_eq_self:
      status: failed
      reason: "Module has no attribute 'Timer.__init__.__code__.co_varnames[0].__eq__'"
    threading_extra128.threadingtimer128.timer_init_co_varnames_1_eq_interval:
      status: failed
      reason: "Module has no attribute 'Timer.__init__.__code__.co_varnames[1].__eq__'"
    threading_extra128.threadingtimer128.timer_run_co_varnames_0_eq_self:
      status: failed
      reason: "Module has no attribute 'Timer.run.__code__.co_varnames[0].__eq__'"
    threading_extra128.threadingtimer128.timer_cancel_co_varnames_0_eq_self:
      status: failed
      reason: "Module has no attribute 'Timer.cancel.__code__.co_varnames[0].__eq__'"
    threading_extra128.timer_init_co_varnames_0:
      status: failed
      reason: "Module has no attribute 'Timer.__init__.__code__.co_varnames[0].__eq__'"
    threading_extra128.timer_init_co_varnames_1:
      status: failed
      reason: "Module has no attribute 'Timer.__init__.__code__.co_varnames[1].__eq__'"
