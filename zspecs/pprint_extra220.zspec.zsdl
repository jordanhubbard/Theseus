package theseus_pprint_cr
version 2
invariant pprint2_pformat {
    call pformat({'a': 1, 'b': 2})
    returns_type str
}
invariant pprint2_nested {
    call pformat([1, [2, [3]]])
    returns_type str
}
invariant pprint2_isreadable {
    call isreadable([1, 2, 3])
    returns True
}
