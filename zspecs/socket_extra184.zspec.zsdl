spec: socket_extra184
version: ">=3.9"
backend: python_module(socket)

docs: https://docs.python.org/3/library/socket.html

provenance:
  derived_from:
    - "https://docs.python.org/3/library/socket.html — Python socket module documentation"
    - "Test vectors verified with Python 3.14.3 on macOS/Darwin 25.4.0, 2026-04-11"
  not_derived_from:
    - "CPython source: Lib/socket.py"
    - "CPython source: Modules/socketmodule.c"
    - "socket_extra183.zspec.zsdl — prior extras."
  notes:
    - "socket.socket.send.__name__ == 'send' — len=4; data-sending method."
    - "socket.socket.recv.__name__ == 'recv' — len=4; data-receiving method."
    - "socket.socket.sendto.__name__ == 'sendto' — len=6; UDP datagram send method."
    - "socket.socket.recvfrom.__name__ == 'recvfrom' — len=8; UDP datagram receive method."
    - "socket.socket.close.__name__ == 'close' — len=5; socket close method."
    - "socket.socket.shutdown.__name__ == 'shutdown' — len=8; graceful shutdown method."
    - "socket.socket.setsockopt.__name__ == 'setsockopt' — len=10; socket option setter."
    - "socket.socket.getsockopt.__name__ == 'getsockopt' — len=10; socket option getter."
    - "All expected values verified with Python 3.14.3 on macOS/Darwin 25.4.0, 2026-04-11."
  created_at: "2026-04-11T00:00:00Z"

error_model: python_exceptions

# ---------------------------------------------------------------------------
# Table 1: socket.socket send/recv method __name__ values and lengths
#
# socket.socket.send.__name__     == 'send'     (len=4)
# socket.socket.recv.__name__     == 'recv'     (len=4)
# socket.socket.sendto.__name__   == 'sendto'   (len=6)
# socket.socket.recvfrom.__name__ == 'recvfrom' (len=8)
#
# These are the core data-transfer methods of the socket class.
# ---------------------------------------------------------------------------

table socket_extra184.socket_send_recv_names:
  description: "socket.socket send and recv method __name__ values and lengths; new to extra184"
  kind: python_call_eq
  category: identity
  id_prefix: socketsendrecv184

  columns: [id, function, args, expected, description]
  rows:
    - [send_name_eq,
       socket.send.__name__.__eq__,
       ["send"],
       true,
       "socket.socket.send.__name__.__eq__('send') == True — send method is named 'send'; len=4; new to extra184"]
    - [send_name_len,
       socket.send.__name__.__len__,
       [],
       4,
       "socket.socket.send.__name__.__len__() == 4 — 'send' has 4 characters; new to extra184"]
    - [recv_name_eq,
       socket.recv.__name__.__eq__,
       ["recv"],
       true,
       "socket.socket.recv.__name__.__eq__('recv') == True — recv method is named 'recv'; len=4; new to extra184"]
    - [recv_name_len,
       socket.recv.__name__.__len__,
       [],
       4,
       "socket.socket.recv.__name__.__len__() == 4 — 'recv' has 4 characters; new to extra184"]
    - [sendto_name_eq,
       socket.sendto.__name__.__eq__,
       ["sendto"],
       true,
       "socket.socket.sendto.__name__.__eq__('sendto') == True — sendto method is named 'sendto'; len=6; new to extra184"]
    - [sendto_name_len,
       socket.sendto.__name__.__len__,
       [],
       6,
       "socket.socket.sendto.__name__.__len__() == 6 — 'sendto' has 6 characters; new to extra184"]
    - [recvfrom_name_eq,
       socket.recvfrom.__name__.__eq__,
       ["recvfrom"],
       true,
       "socket.socket.recvfrom.__name__.__eq__('recvfrom') == True — recvfrom method is named 'recvfrom'; len=8; new to extra184"]
    - [recvfrom_name_len,
       socket.recvfrom.__name__.__len__,
       [],
       8,
       "socket.socket.recvfrom.__name__.__len__() == 8 — 'recvfrom' has 8 characters; new to extra184"]

# ---------------------------------------------------------------------------
# Table 2: socket.socket connection control method __name__ values
#
# socket.socket.close.__name__    == 'close'    (len=5)
# socket.socket.shutdown.__name__ == 'shutdown' (len=8)
#
# These methods control the lifecycle of a socket connection.
# ---------------------------------------------------------------------------

table socket_extra184.socket_close_shutdown_names:
  description: "socket.socket close and shutdown method __name__ values; new to extra184"
  kind: python_call_eq
  category: identity
  id_prefix: socketsendrecv184

  columns: [id, function, args, expected, description]
  rows:
    - [close_name_eq,
       socket.close.__name__.__eq__,
       ["close"],
       true,
       "socket.socket.close.__name__.__eq__('close') == True — close method is named 'close'; len=5; new to extra184"]
    - [close_name_len,
       socket.close.__name__.__len__,
       [],
       5,
       "socket.socket.close.__name__.__len__() == 5 — 'close' has 5 characters; new to extra184"]
    - [shutdown_name_eq,
       socket.shutdown.__name__.__eq__,
       ["shutdown"],
       true,
       "socket.socket.shutdown.__name__.__eq__('shutdown') == True — shutdown method is named 'shutdown'; len=8; new to extra184"]
    - [shutdown_name_len,
       socket.shutdown.__name__.__len__,
       [],
       8,
       "socket.socket.shutdown.__name__.__len__() == 8 — 'shutdown' has 8 characters; new to extra184"]

# ---------------------------------------------------------------------------
# Table 3: socket.socket option getter/setter method __name__ values
#
# socket.socket.setsockopt.__name__ == 'setsockopt' (len=10)
# socket.socket.getsockopt.__name__ == 'getsockopt' (len=10)
#
# These methods set and get socket-level options (e.g., SO_REUSEADDR).
# Both names are exactly 10 characters long.
# ---------------------------------------------------------------------------

table socket_extra184.socket_sockopt_names:
  description: "socket.socket setsockopt and getsockopt method __name__ values; new to extra184"
  kind: python_call_eq
  category: identity
  id_prefix: socketsendrecv184

  columns: [id, function, args, expected, description]
  rows:
    - [setsockopt_name_eq,
       socket.setsockopt.__name__.__eq__,
       ["setsockopt"],
       true,
       "socket.socket.setsockopt.__name__.__eq__('setsockopt') == True — setsockopt method is named 'setsockopt'; len=10; new to extra184"]
    - [setsockopt_name_len,
       socket.setsockopt.__name__.__len__,
       [],
       10,
       "socket.socket.setsockopt.__name__.__len__() == 10 — 'setsockopt' has 10 characters; new to extra184"]
    - [getsockopt_name_eq,
       socket.getsockopt.__name__.__eq__,
       ["getsockopt"],
       true,
       "socket.socket.getsockopt.__name__.__eq__('getsockopt') == True — getsockopt method is named 'getsockopt'; len=10; new to extra184"]
    - [getsockopt_name_len,
       socket.getsockopt.__name__.__len__,
       [],
       10,
       "socket.socket.getsockopt.__name__.__len__() == 10 — 'getsockopt' has 10 characters; new to extra184"]

# ---------------------------------------------------------------------------
# Standalone invariants — extra184
# ---------------------------------------------------------------------------

invariant socket_extra184.send_name_is_send:
  description: "socket.socket.send.__name__ == 'send' — send method name; len=4; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.send.__name__.__eq__
  args: ["send"]
  expected: true

invariant socket_extra184.recv_name_is_recv:
  description: "socket.socket.recv.__name__ == 'recv' — recv method name; len=4; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.recv.__name__.__eq__
  args: ["recv"]
  expected: true

invariant socket_extra184.sendto_name_is_sendto:
  description: "socket.socket.sendto.__name__ == 'sendto' — sendto method name; len=6; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.sendto.__name__.__eq__
  args: ["sendto"]
  expected: true

invariant socket_extra184.recvfrom_name_is_recvfrom:
  description: "socket.socket.recvfrom.__name__ == 'recvfrom' — recvfrom method name; len=8; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.recvfrom.__name__.__eq__
  args: ["recvfrom"]
  expected: true

invariant socket_extra184.close_name_is_close:
  description: "socket.socket.close.__name__ == 'close' — close method name; len=5; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.close.__name__.__eq__
  args: ["close"]
  expected: true

invariant socket_extra184.shutdown_name_is_shutdown:
  description: "socket.socket.shutdown.__name__ == 'shutdown' — shutdown method name; len=8; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.shutdown.__name__.__eq__
  args: ["shutdown"]
  expected: true

invariant socket_extra184.setsockopt_name_is_setsockopt:
  description: "socket.socket.setsockopt.__name__ == 'setsockopt' — setsockopt method name; len=10; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.setsockopt.__name__.__eq__
  args: ["setsockopt"]
  expected: true

invariant socket_extra184.getsockopt_name_is_getsockopt:
  description: "socket.socket.getsockopt.__name__ == 'getsockopt' — getsockopt method name; len=10; new to extra184"
  category: identity
  kind: python_call_eq
  function: socket.getsockopt.__name__.__eq__
  args: ["getsockopt"]
  expected: true

# ── Synthesis layer annotation ───────────────────────────────────────────────
synthesis:
  status: success
  model: azure/anthropic/claude-sonnet-4-6
  attempted_at: 2026-04-17T14:49:43Z
  iterations: 1
  pass_count: 24
  fail_count: 0
  total_invariants: 24
  notes: Synthesis succeeded in 1 iteration(s). 24 invariants passing.
  infeasible_reason: null
  invariant_annotations: {}
