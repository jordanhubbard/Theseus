# Core Python tooling used by local development, tests, and documentation.
#
# The Theseus runtime remains stdlib-only. These packages are required for
# committed Makefile targets such as `make test`, `make compile-zsdl`, and
# `make docs`.
pytest>=8.0,<9
PyYAML>=6.0,<7
mkdocs-material>=9.5,<10
